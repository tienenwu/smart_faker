name: smart_faker
description: "An intelligent test data generator for Flutter/Dart with schema-based generation, smart relationships, and comprehensive internationalization support."
version: 0.3.3
homepage: https://github.com/tienenwu/smart_faker
repository: https://github.com/tienenwu/smart_faker
# publisher: tienenwu.me

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.7.0
  intl: ^0.17.0
  crypto: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.20.0
  very_good_analysis: ^2.4.0
  coverage: ^1.3.0
  build_runner: ^2.1.0
  json_serializable: ^6.2.0

flutter: